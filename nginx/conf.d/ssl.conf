# Optional TLS server for localhost (requires certs mounted at /etc/nginx/certs)
# To enable, mount this file and certificates and adjust docker-compose to expose 443.
# Example: docker run -v ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro -v ./nginx/conf.d:/etc/nginx/conf.d:ro -v ./certs:/etc/nginx/certs:ro -p 443:443 ...
# For production, use real certificates (e.g., via Let's Encrypt) and proper domain names.

# server {
#   listen 443 ssl http2;
#   server_name localhost;
#   ssl_certificate     /etc/nginx/certs/server.crt;
#   ssl_certificate_key /etc/nginx/certs/server.key;
#   ssl_session_timeout 1d;
#   ssl_protocols TLSv1.2 TLSv1.3;
#   ssl_ciphers 'ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256';
#   ssl_prefer_server_ciphers off;
#   location / {
#     return 301 http://$host$request_uri; # change to proxy_pass to app if you want HTTPS origin in dev
#   }
# }
