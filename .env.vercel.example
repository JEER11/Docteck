# Frontend env for Vercel (Create React App)
# Import these into Vercel → Project Settings → Environment Variables.
# IMPORTANT: Any key starting with REACT_APP_ is embedded into the JS bundle at build time
# and is public. Only include non-secret values here (Firebase web config, publishable keys, flags).

# Where the app is served (your site uses /app on Vercel)
PUBLIC_URL=/app/

# API base used by the frontend (optional if you proxy via the same origin)
# If your backend lives on another host, set it here; otherwise you can omit this.
REACT_APP_API_URL=https://api.yourdomain.com

# Firebase web config (public client values)
REACT_APP_FIREBASE_API_KEY=
REACT_APP_FIREBASE_AUTH_DOMAIN=
REACT_APP_FIREBASE_PROJECT_ID=
REACT_APP_FIREBASE_APP_ID=
REACT_APP_FIREBASE_STORAGE_BUCKET=
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=
REACT_APP_FIREBASE_MEASUREMENT_ID=

# Stripe publishable key (public)
REACT_APP_STRIPE_PUBLISHABLE_KEY=

# Auth provider toggles (show/hide buttons in UI)
REACT_APP_ENABLE_GOOGLE=true
REACT_APP_ENABLE_FACEBOOK=false
REACT_APP_ENABLE_MICROSOFT=false
REACT_APP_ENABLE_YAHOO=false

# Optional: Weather widget (OpenWeatherMap API key)
# Create a free key at https://openweathermap.org/ and consider restricting usage
REACT_APP_OPENWEATHER_API_KEY=

# Keep source maps off in production bundles
GENERATE_SOURCEMAP=false
